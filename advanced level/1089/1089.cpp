#include<stdio.h> 
#include<iostream>
#include<vector>
using namespace std; 

int n;
vector<int> a,b,ans;
vectot< vetor<int> > m;

bool ise(vector<int> &x,vector<int> &y){
	int xx = x.size();
	for(int i=0;i<xx;i++){
		if(x[i]!=y[i]) return false;
	}
	return true;
}

vector<int> lsort(vector<int> &x,vector<int> &y){
	vector<int> t;
	xl = x.size();yl=y.size();
	p1=0;p2=0;
	while(p1<xl&&p2<yl){
		if(x[p1]<y[p2]) 
	}
} 

void mergeSort(vector<int> &x){
	int l = x.size();
	int q = 1;
	int p = 0;
	vector<int> temp;
	while(p<l){
		
	}
}


int main()	{
	scanf("%d",&n);
	for(int i=0;i<n;i++){
		int x;
		vector<int> t;
		t.clear();
		scanf("%d",&x);
		t.push_back(x);
		m.push_back(t);
	}
	for(int i=0;i<n;i++){
		int x;
		vector<int> t;
		scanf("%d",&x);
		a.push_back(x);
	}
	
}
